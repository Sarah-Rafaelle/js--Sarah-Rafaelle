} function setup() {
  createCanvas(800, 700);
  background("blue");
}

function draw() {
  
  stroke("purple");
  fill("pink");
  
  //consele.log (mouseIsPressed);
  
  if(mouseIsPressed){
  rect(mouseX, mouseY, 20, 25); 
   }
